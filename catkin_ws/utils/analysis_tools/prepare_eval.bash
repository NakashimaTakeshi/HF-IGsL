
mkdir -p "Path A"
mkdir -p "Path B"
mkdir -p "Path C"
mkdir -p "Path D"
mkdir -p "Path E"
mkdir -p "Path F"
mkdir -p "Path G"
mkdir -p "Path H"
mkdir -p "Path I"
mkdir -p "Path J"
mkdir -p "Path K"

mv *dataset1_* "Path A"/
mv *dataset2_* "Path B"/
mv *dataset4_* "Path C"/
mv *dataset5_* "Path D"/
mv *dataset6_* "Path E"/
mv *dataset7_* "Path F"/
mv *dataset8_* "Path G"/
mv *dataset9_* "Path H"/
mv *dataset10_* "Path I"/
mv *dataset11_* "Path J"/
mv *dataset12_* "Path K"/